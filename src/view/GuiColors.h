#pragma once
#include <SFML/Graphics/Color.hpp>

namespace GUI {
    const sf::Color BACKGROUND = sf::Color(40, 44, 52);
    const sf::Color TEXT = sf::Color(171, 178, 191);
    const sf::Color COMMENT = sf::Color(92, 99, 112);
    const sf::Color CURSOR = sf::Color(171, 178, 191);
    const sf::Color LINE_NUMBER = sf::Color(92, 99, 112);
    const sf::Color SELECTION = sf::Color(62, 68, 81);
    const sf::Color STATUS_BG = sf::Color(30, 33, 39);
    const sf::Color ERROR = sf::Color(224, 108, 117);
    const sf::Color SUCCESS = sf::Color(152, 195, 121);
}
